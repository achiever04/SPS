# ============================================================
#  HFT Trading Bot v2 — Production Dependencies
# ============================================================
#  Live pipeline (no Pandas, minimal footprint):
#      pip install aiohttp onnxruntime numpy python-dotenv joblib
#
#  Full install (including training + backtest tools):
#      pip install -r requirements.txt
# ============================================================

# ── Live trading pipeline ────────────────────────────────────────────────
aiohttp>=3.9.0
onnxruntime>=1.17.0
numpy>=1.26.0
python-dotenv>=1.0.0
joblib>=1.3.0

# ── Model training & backtest (offline only — not imported by live bot) ──
lightgbm>=4.1.0
scikit-learn>=1.4.0
skl2onnx>=1.16.0
pandas>=2.1.0          # used in train_model.py data loader only

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
